<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"  
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical">
                  
	          <ImageView android:id="@+id/profile_contact_item_icon"
	                   android:layout_width="wrap_content"
	                   android:layout_height="wrap_content"
	                   android:layout_marginLeft="8dip"
	                   android:layout_centerVertical="true"/>
	                   
                 <LinearLayout android:id="@+id/id_contact_info"
	               android:layout_width="fill_parent"
	               android:layout_height="46dip"
	               android:layout_centerVertical="true"
		           android:layout_toLeftOf="@+id/request_change_icon"
		           android:layout_toRightOf="@id/profile_contact_item_icon"
		           android:paddingLeft="8dip"
		           android:orientation="vertical"
		           android:gravity="center_vertical">
	           
	          <TextView android:id="@+id/profile_contact_item_tv"
				       android:layout_width="fill_parent"
				       android:layout_height="wrap_content"
				       android:gravity="center_vertical"/>
                  
			       <TextView android:id="@+id/contact_bind"
			       android:layout_width="fill_parent"
			       android:layout_height="wrap_content"
			       android:layout_marginTop="4dip"
			       android:layout_marginBottom="4dip"
			       android:textColor="#639639"
			       android:textSize="12dip"
			       android:gravity="center_vertical"
			       android:visibility="gone"/>
                  </LinearLayout>
    
			       
					       
       <ImageView
       android:id="@id/request_change_icon"
       android:layout_width="38dip"
       android:layout_height="38dip"
       android:layout_alignParentRight="true"
       android:layout_centerVertical="true"
       android:layout_marginRight="6dip"
       android:src="@drawable/ic_settings"/>
</RelativeLayout>