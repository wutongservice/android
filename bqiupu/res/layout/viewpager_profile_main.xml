<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" 
    android:background="@color/bpc_backgroud">

    <LinearLayout
        android:id="@+id/titlebar_container"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/title_bar_height"
        android:orientation="horizontal" >
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />
    
    <com.borqs.qiupu.fragment.FixedTabsView
        android:id="@+id/indicator"
        style="@style/fix_tab_style" />
    

    <!-- <include layout="@layout/bpc_users_account_detail_header" -->
    <!-- android:layout_below="@id/titlebar_container" -->
    <!-- android:id="@+id/profile_span" -->
    <!-- android:layout_width="fill_parent" -->
    <!-- android:layout_height="wrap_content" -->
    <!-- /> -->

   
<!--         android:layout_above="@+id/bpc_users_account_detail_footer" -->

   <!--  <com.borqs.qiupu.fragment.TitlePageIndicator
        android:id="@+id/indicator"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/bpc_users_account_detail_footer"
        android:background="@drawable/tab_bg"
        android:padding="10dip" /> -->


    <!-- <include layout="@layout/bpc_users_account_detail_footer" -->
    <!-- android:id="@+id/bottom_span" -->
    <!-- android:layout_alignParentBottom="true" -->
    <!-- android:layout_below="@id/content_span" -->
    <!-- /> -->

</LinearLayout>